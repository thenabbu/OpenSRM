[project]
name = "opensrm"
version = "1.0.0"
description = "Self-hosted attendance dashboard for the SRM Student Portal"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "flask==3.1.3",
    "playwright==1.63.0",
    "ddddocr==1.6.1",
    "gunicorn==26.2.0",
    "cryptography>=50.0.1",
    "blobatar[flask]",
]

[project.scripts]

[tool.uv.sources]
blobatar = { git = "https://github.com/thenabbu/blobatar", subdirectory = "packages/python", rev = "feat/python-sdk" }
